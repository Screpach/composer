import { useStudioStore } from '../store/useStudioStore';
import { audioEngine } from '../audio/audioEngine';
export function NoteEntryInspector(){const s=useStudioStore();
return <div className='rounded-3xl bg-white/35 p-4 space-y-3'><h3 className='font-semibold'>Note entry</h3><label className='block'>Voice<select className='w-full rounded' value={s.selectedVoiceId} onChange={e=>s.setVoice(e.target.value)}>{s.voices.map(v=><option key={v.id} value={v.id}>{v.name}</option>)}</select></label><label className='block'>Duration<select className='w-full rounded' value={s.selectedDuration} onChange={e=>s.setDuration(e.target.value as any)}><option>whole</option><option>half</option><option>quarter</option><option>eighth</option><option>sixteenth</option></select></label><div className='flex gap-2'><button onClick={()=>s.setOctave(s.octave-1)}>-</button><span>Octave {s.octave}</span><button onClick={()=>s.setOctave(s.octave+1)}>+</button></div><button onClick={()=>s.toggle('restMode')}>Rest {s.restMode?'On':'Off'}</button><button onClick={()=>s.toggle('tied')}>Tie/slur {s.tied?'On':'Off'}</button><button onClick={()=>s.toggle('stepEntry')}>Step entry {s.stepEntry?'On':'Off'}</button><label>Volume<input type='range' min={0} max={1} step={0.01} value={s.masterVolume} onChange={e=>{const v=+e.target.value;s.setVolume(v);audioEngine.setVolume(v);}}/></label><label>Waveform<select value={s.waveform} onChange={e=>s.setWaveform(e.target.value as OscillatorType)}><option>sine</option><option>triangle</option><option>sawtooth</option><option>square</option></select></label></div>}
