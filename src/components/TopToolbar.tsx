import { Play, Square, Settings, ArrowLeft, ArrowRight } from 'lucide-react';
import { GlassButton } from './GlassButton';
import { useStudioStore } from '../store/useStudioStore';
export function TopToolbar({onPlay,onStop}:{onPlay:()=>void;onStop:()=>void}){const s=useStudioStore();return <div className='flex flex-wrap gap-2 items-center'><GlassButton onClick={()=>s.toggle('keepSounding')}>Keep sounding {s.keepSounding?'On':'Off'}</GlassButton><GlassButton onClick={onPlay}><Play size={16}/></GlassButton><GlassButton onClick={onStop}><Square size={16}/></GlassButton><GlassButton>●</GlassButton><GlassButton onClick={()=>s.moveCursor(-1)}><ArrowLeft size={16}/></GlassButton><GlassButton onClick={()=>s.moveCursor(1)}><ArrowRight size={16}/></GlassButton><label className='text-sm'>Tempo <input type='number' className='ml-1 w-20 rounded' value={s.tempo} onChange={e=>s.setTempo(+e.target.value)}/></label><span className='rounded-full bg-white/50 px-3 py-1 text-xs'>Tuning: 31-EDO</span><span className='rounded-full bg-white/50 px-3 py-1 text-xs'>Step entry: {s.stepEntry?'On':'Off'}</span><GlassButton><Settings size={16}/></GlassButton></div>}
